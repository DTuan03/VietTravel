<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu nền (white) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#D7D2D2"/>
        </shape>
    </item>

    <!-- Viền dưới -->
    <item
        android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/white"/>
        </shape>
    </item>
</layer-list>
